---
phase: 01-pipeline-leads
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-score-ai.json
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-notify-hot.json
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/PIPELINE-ACTIVATION.md
autonomous: false
requirements: [LEAD-01, LEAD-04, LEAD-06]

user_setup:
  - service: PhantomBuster
    why: "LinkedIn Sales Navigator extraction"
    env_vars:
      - name: PHANTOMBUSTER_API_KEY
        source: "PhantomBuster Dashboard -> Account -> API Keys"
      - name: PB_SALES_NAV_AGENT_ID
        source: "PhantomBuster Dashboard -> Create Sales Navigator Search Export phantom -> copy Agent ID"
    dashboard_config:
      - task: "Create Sales Navigator Search Export phantom"
        location: "PhantomBuster Dashboard -> Automations -> Sales Navigator Search Export"
      - task: "Configure Sales Nav search URL for Chile B2B"
        location: "Phantom config -> LinkedIn Sales Nav search URL with Chile + B2B filters"
  - service: SimpleAPI
    why: "SII RUT enrichment"
    env_vars:
      - name: SIMPLE_API_KEY
        source: "SimpleAPI.cl -> Mi cuenta -> API Key"
  - service: Telegram
    why: "HOT lead notifications"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "Telegram -> @BotFather -> /newbot -> copy token"
      - name: TELEGRAM_CHAT_ID
        source: "Send message to bot, then GET https://api.telegram.org/bot{TOKEN}/getUpdates -> extract chat.id"
  - service: n8n
    why: "All workflow variables"
    env_vars:
      - name: SAAN_CONVEX_SITE_URL
        source: "Convex Dashboard -> Deployment -> Site URL"
      - name: SAAN_API_SECRET
        source: "Convex Dashboard -> Settings -> Environment Variables"

must_haves:
  truths:
    - "PhantomBuster extraction workflow is configured with real Sales Nav search URL"
    - "Gemini scoring workflow uses correct auth header and scores leads HOT/WARM/NURTURE/SKIP"
    - "HOT leads trigger immediate Telegram notification to CEO"
    - "Full pipeline runs end-to-end: extract -> enrich -> score -> notify"
  artifacts:
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-score-ai.json"
      provides: "Fixed scoring workflow with correct auth header"
      contains: "X-SAAN-Secret"
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-notify-hot.json"
      provides: "Telegram notification workflow for HOT leads"
      contains: "telegram"
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/PIPELINE-ACTIVATION.md"
      provides: "Step-by-step activation checklist"
  key_links:
    - from: "saan-leads-score-ai.json"
      to: "Convex leads:updateLeadScore"
      via: "HTTP POST with X-SAAN-Secret"
      pattern: "leads:updateLeadScore"
    - from: "saan-leads-notify-hot.json"
      to: "Telegram Bot API"
      via: "n8n Telegram node"
      pattern: "telegram"
    - from: "scoring workflow"
      to: "notification workflow"
      via: "leads marked HOT trigger notification check"
      pattern: "scoreCategory.*HOT"
---

<objective>
Fix scoring workflow auth, create HOT lead Telegram notifications, and produce the pipeline activation checklist. This is the final plan that makes the entire pipeline operational end-to-end.

Purpose: LEAD-01 (PhantomBuster active), LEAD-04 (Gemini scoring), and LEAD-06 (HOT notifications) all depend on having a working pipeline. The scoring workflow has the Bearer auth bug, Telegram notifications don't exist yet, and PhantomBuster needs real configuration.
Output: Fixed scoring workflow, new notification workflow, activation checklist for human setup.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-leads/01-RESEARCH.md
@.planning/phases/01-pipeline-leads/01-01-SUMMARY.md

Source files:
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-score-ai.json
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-discover-phantombuster.json
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/http.ts

<interfaces>
<!-- From leads.ts -->
updateLeadScore(args: {
  leadId: Id<"leads">,
  score: number,
  scoreCategory: string,  // "HOT" | "WARM" | "NURTURE" | "SKIP"
  scoreReasoning?: string,
  scoreVersion?: number,
  scoredBySkill?: string
})

getLeadsToScore() -> leads with status "enriched", limit 20

<!-- From telegramQueue.ts -->
enqueue(args: {
  messageType: string,  // "alert"
  agentId: string,      // "leads"
  priority: string,     // "high"
  content: string,      // HTML formatted
  status: string,       // "pending"
  retryCount: number,   // 0
  createdAt: number
})

<!-- Auth pattern (fixed in 01-01) -->
Header: X-SAAN-Secret: {secret}
OR: Authorization: Bearer {secret}
Both accepted after 01-01 fix.

<!-- Telegram message template (from research) -->
Lead HOT format: empresa, contacto, cargo, score, reasoning, linkedinUrl
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scoring workflow auth + create HOT notification workflow</name>
  <files>
    C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-score-ai.json
    C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-notify-hot.json
  </files>
  <action>
  **Part A — Fix saan-leads-score-ai.json:**
  Read the existing scoring workflow JSON. Find ALL HTTP Request nodes that call Convex and check their auth headers. Any node using `Authorization: Bearer` must be changed to use `X-SAAN-Secret` header instead (matching the http.ts convention). Specifically:
  - Find the header configuration in each HTTP Request node
  - Replace `"name": "Authorization", "value": "Bearer {{$vars.SAAN_API_SECRET}}"` with `"name": "X-SAAN-Secret", "value": "={{$vars.SAAN_API_SECRET}}"`
  - Preserve all other workflow logic unchanged

  Also verify the scoring workflow calls `leads:getLeadsToScore` (to get enriched leads) and `leads:updateLeadScore` (to save scores). These should already be the function names.

  **Part B — Create saan-leads-notify-hot.json:**
  Create a new n8n workflow JSON for HOT lead Telegram notifications:

  1. **Schedule Trigger** — Every 30 minutes (check for newly scored HOT leads)
  2. **Set Config** — $vars: SAAN_CONVEX_SITE_URL, SAAN_API_SECRET, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
  3. **Get Scored Leads** — HTTP POST to Convex: `{ "function": "leads:getLeadsByStatus", "args": { "status": "scored", "limit": 20 } }`
  4. **Filter HOT Only** — IF node: scoreCategory === "HOT"
  5. **Format Message** — Code node producing Telegram HTML message:
     ```
     Lead HOT Detectado

     Empresa: {empresa}
     Contacto: {contacto} ({cargo})
     Score: {score}/100
     Industria: {industria}

     Razon: {scoreReasoning}

     Ver en LinkedIn: {linkedinUrl}
     ```
  6. **Send Telegram** — HTTP Request POST to `https://api.telegram.org/bot{{$vars.TELEGRAM_BOT_TOKEN}}/sendMessage` with body `{ "chat_id": "{{$vars.TELEGRAM_CHAT_ID}}", "text": formatted_message, "parse_mode": "HTML" }`
  7. **Mark Notified** — HTTP POST to Convex: `{ "function": "leads:updateLeadStatus", "args": { "leadId": "...", "status": "outreach_queued" } }` — moves lead out of "scored" so it won't be notified again
  8. **Error Handler** — On error, log to agentMemory

  Use same JSON structure patterns as other workflow files. All credentials via $vars.
  </action>
  <verify>
    <automated>node -e "const s=require('C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-score-ai.json'); const n=require('C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-notify-hot.json'); const sStr=JSON.stringify(s); console.log('score auth fixed:', !sStr.includes('Authorization') || sStr.includes('X-SAAN-Secret')); console.log('notify nodes:', n.nodes.length, 'has telegram:', JSON.stringify(n).includes('telegram'))"</automated>
  </verify>
  <done>Scoring workflow uses X-SAAN-Secret header (not Bearer). HOT notification workflow sends Telegram alerts for HOT leads and moves them to outreach_queued status.</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline activation checklist</name>
  <files>C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/PIPELINE-ACTIVATION.md</files>
  <action>
  Create a comprehensive step-by-step activation checklist in PIPELINE-ACTIVATION.md that covers:

  **Pre-requisites (one-time setup):**
  1. Convex deployment active (`cd SAAN && npx convex deploy`)
  2. SAAN_API_SECRET set in Convex Dashboard Environment Variables
  3. PhantomBuster account created, Sales Navigator Search Export phantom configured with Chile B2B filters, Agent ID obtained
  4. Firecrawl API key obtained
  5. SimpleAPI account created (free tier), API key obtained
  6. Telegram bot created via @BotFather, token obtained
  7. Telegram chat_id obtained (send message to bot, call getUpdates API)

  **n8n Variables to set (Settings -> Variables):**
  - SAAN_CONVEX_SITE_URL
  - SAAN_API_SECRET
  - FIRECRAWL_API_KEY
  - PHANTOMBUSTER_API_KEY
  - PB_SALES_NAV_AGENT_ID
  - SIMPLE_API_KEY
  - TELEGRAM_BOT_TOKEN
  - TELEGRAM_CHAT_ID

  **Workflow import order:**
  1. Import saan-leads-discover-phantombuster.json (but keep INACTIVE until PB agent configured)
  2. Import saan-leads-discover-firecrawl.json
  3. Import saan-leads-enrich.json
  4. Import saan-leads-enrich-sii.json
  5. Import saan-leads-score-ai.json
  6. Import saan-leads-notify-hot.json

  **Activation sequence:**
  1. Activate enrichment workflows first (enrich + enrich-sii)
  2. Activate scoring workflow
  3. Activate notification workflow
  4. Activate discovery workflows (Firecrawl first, then PhantomBuster)
  5. Monitor n8n execution logs for first 24 hours

  **Validation tests:**
  1. Manual test: insert 1 test lead via Convex Dashboard, verify it gets enriched + scored
  2. Verify PhantomBuster phantom runs and produces CSV output
  3. Verify Telegram bot sends test message
  4. Wait 24h, check leads table has new entries

  **Expected pipeline schedule:**
  - PhantomBuster: Mon/Wed/Fri 07:00
  - Firecrawl discovery: daily 06:00
  - Firecrawl enrichment: every 2h
  - SII enrichment: every 4h
  - Scoring: every 3h
  - HOT notifications: every 30min
  </action>
  <verify>
    <automated>test -f "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/PIPELINE-ACTIVATION.md" && wc -l "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/PIPELINE-ACTIVATION.md"</automated>
  </verify>
  <done>PIPELINE-ACTIVATION.md exists with complete checklist covering: prerequisites, n8n variables, import order, activation sequence, validation tests, and expected schedule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify pipeline end-to-end activation</name>
  <action>
  User verifies the complete lead pipeline is operational by following the activation checklist and confirming data flows through all stages.
  </action>
  <what-built>
  Complete lead pipeline: HTTP bugfixes (Plan 01), SII enrichment (Plan 02), scoring auth fix, Telegram notifications, and activation checklist. All workflow JSONs are ready to import into n8n.
  </what-built>
  <how-to-verify>
  1. Review PIPELINE-ACTIVATION.md for completeness
  2. Follow the pre-requisites section to set up external services
  3. Import workflows into n8n (sistecotest.app.n8n.cloud)
  4. Set all n8n variables
  5. Run smoke test: `SAAN_CONVEX_SITE_URL=https://xxx.convex.site SAAN_API_SECRET=xxx bash SAAN/tests/smoke-phase1.sh`
  6. Activate workflows per activation sequence
  7. Insert 1 test lead manually in Convex Dashboard, wait for enrichment + scoring cycle
  8. Verify Telegram notification arrives for any HOT-scored test lead
  </how-to-verify>
  <resume-signal>Type "pipeline-active" once workflows are running and first lead has been processed, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- Scoring workflow JSON uses X-SAAN-Secret (not Authorization Bearer)
- Notification workflow sends Telegram message for HOT leads
- Notification workflow moves notified leads to outreach_queued (prevents re-notification)
- PIPELINE-ACTIVATION.md covers all env vars, import order, and validation steps
- All workflow JSONs are valid and parseable
</verification>

<success_criteria>
- Scoring workflow auth header matches http.ts convention (X-SAAN-Secret)
- HOT leads trigger Telegram notification with empresa, contacto, score, reasoning
- Activation checklist is complete and actionable
- User confirms pipeline is running (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-leads/01-03-SUMMARY.md`
</output>

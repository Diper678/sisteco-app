---
phase: 01-pipeline-leads
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich.json
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich-sii.json
autonomous: true
requirements: [LEAD-03]

must_haves:
  truths:
    - "Leads get enriched with SII data (RUT, razon social, giro, domicilio) automatically"
    - "SII enrichment only runs for leads without existing SII data (no wasted API calls)"
    - "Free tier rate limits are respected (max 10 calls/month cached)"
  artifacts:
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich-sii.json"
      provides: "n8n workflow for SII enrichment via SimpleAPI"
      contains: "simpleapi"
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich.json"
      provides: "Updated enrichment workflow that triggers SII enrichment"
  key_links:
    - from: "saan-leads-enrich-sii.json"
      to: "SimpleAPI RUT endpoint"
      via: "HTTP Request node"
      pattern: "simpleapi\\.cl"
    - from: "saan-leads-enrich-sii.json"
      to: "leads:enrichLead"
      via: "HTTP POST to Convex"
      pattern: "leads:enrichLead"
---

<objective>
Create SII enrichment workflow that queries SimpleAPI for Chilean company data (RUT, razon social, giro, domicilio) and stores it in lead enrichedData. This is the key differentiator -- data no international competitor has.

Purpose: LEAD-03 requires enrichment with SII data. Firecrawl enrichment already exists; this adds the Chilean-specific SII layer that makes Sisteco's data unique.
Output: New n8n workflow JSON for SII enrichment, plus update to existing enrich workflow to chain SII enrichment after Firecrawl.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-pipeline-leads/01-RESEARCH.md

Source files:
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich.json

<interfaces>
<!-- From leads.ts - enrichLead mutation -->
enrichLead(args: { leadId: Id<"leads">, enrichedData: any, enrichedBySkill?: string })
  -> Updates lead with enrichedData, sets status to "enriched"
  -> Auto-populates industria, tamano, ubicacion from enrichedData fields

<!-- SimpleAPI RUT endpoint (from research) -->
GET https://api.simpleapi.cl/api/rut/buscar?q={empresa_name}
Headers: { Authorization: Bearer {SIMPLE_API_KEY} }
Response: { results: [{ rut, razon_social, giro, domicilio }] }

<!-- n8n variable pattern (from existing workflows) -->
All credentials via $vars: SAAN_CONVEX_SITE_URL, SAAN_API_SECRET, SIMPLE_API_KEY (new)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SII enrichment workflow JSON</name>
  <files>C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich-sii.json</files>
  <action>
  Create a new n8n workflow JSON file `saan-leads-enrich-sii.json` following the same structure/patterns as existing workflows in the n8n-workflows/ directory. The workflow should:

  **Trigger:** Schedule trigger every 4 hours (staggered from the 2-hour Firecrawl enrich to avoid overlap)

  **Flow:**
  1. **Set Config** — Set node with n8n $vars: SAAN_CONVEX_SITE_URL, SAAN_API_SECRET, SIMPLE_API_KEY
  2. **Get Enriched Leads** — HTTP Request POST to `{{$json.convexUrl}}/api/call` with body `{ "function": "leads:getLeadsByStatus", "args": { "status": "enriched", "limit": 10 } }` using X-SAAN-Secret header. These are leads that have Firecrawl data but may not have SII data yet.
  3. **Filter No SII** — IF node: check `$json.enrichedData.sii_validated` is NOT true (only enrich leads without SII data)
  4. **Rate Limit Check** — Code node: check a counter variable to ensure we don't exceed 10 SimpleAPI calls/month on free tier. If limit reached, stop with a message. Use n8n static data to track monthly count.
  5. **SimpleAPI Lookup** — HTTP Request GET to `https://api.simpleapi.cl/api/rut/buscar?q={{encodeURIComponent($json.empresa)}}` with Authorization Bearer header
  6. **Parse SII Response** — Code node: extract first result (rut, razon_social, giro, domicilio) or mark as sii_validated:false if no match
  7. **Update Lead** — HTTP Request POST to Convex `/api/call` with body `{ "function": "leads:enrichLead", "args": { "leadId": "...", "enrichedData": { ...existing enrichedData, sii_rut, sii_razon_social, sii_giro, sii_domicilio, sii_validated: true } } }`
  8. **Wait** — Wait node 3 seconds between API calls to respect rate limits
  9. **Error Handler** — On error, log to agentMemory via Convex call

  Use the exact same JSON structure as the other workflow files (with "meta", "nodes", "connections", "settings" top-level keys). Reference `$vars.SAAN_CONVEX_SITE_URL`, `$vars.SAAN_API_SECRET`, `$vars.SIMPLE_API_KEY` for credentials.

  IMPORTANT: The SimpleAPI endpoint format may need adjustment after manual testing (research confidence: MEDIUM). Include a comment node at the top of the workflow noting "Verify SimpleAPI endpoint format before activating".
  </action>
  <verify>
    <automated>node -e "const w=require('C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich-sii.json'); console.log('nodes:', w.nodes.length, 'valid:', !!w.nodes && !!w.connections)"</automated>
  </verify>
  <done>saan-leads-enrich-sii.json is valid JSON with 8+ nodes, SimpleAPI lookup, rate limit guard, and proper Convex integration. Uses $vars for all credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Update existing enrich workflow to note SII dependency</name>
  <files>C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich.json</files>
  <action>
  Read the existing saan-leads-enrich.json workflow and add a Sticky Note node documenting the SII enrichment chain:

  Add a sticky note node with content:
  ```
  SII Enrichment runs as a separate workflow (saan-leads-enrich-sii)
  every 4 hours, processing leads with status "enriched" that don't
  have sii_validated data yet. This workflow (Firecrawl enrich) runs
  every 2 hours and feeds leads into "enriched" status for SII pickup.

  Pipeline: new -> [Firecrawl enrich] -> enriched -> [SII enrich] -> enriched (with SII data) -> [Scoring]
  ```

  Do NOT change any functional nodes -- only add the documentation sticky note. The SII workflow runs independently on its own schedule, not chained within this workflow, to keep things decoupled and respect SimpleAPI rate limits.
  </action>
  <verify>
    <automated>node -e "const w=require('C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/n8n-workflows/saan-leads-enrich.json'); const sticky=w.nodes.filter(n=>n.type==='n8n-nodes-base.stickyNote'); console.log('sticky notes:', sticky.length)"</automated>
  </verify>
  <done>Existing enrich workflow has a sticky note documenting the SII enrichment dependency chain. No functional changes made to existing workflow.</done>
</task>

</tasks>

<verification>
- saan-leads-enrich-sii.json is valid JSON parseable by Node.js
- Workflow has SimpleAPI HTTP request node targeting simpleapi.cl
- Workflow has rate limit guard for free tier (10/month)
- Workflow uses $vars for credentials (no hardcoded secrets)
- Existing enrich workflow unchanged except for documentation sticky note
</verification>

<success_criteria>
- New SII enrichment workflow JSON exists and is valid
- Rate limit protection prevents exceeding 10 calls/month on free tier
- Pipeline chain documented: Firecrawl (2h) -> SII (4h) -> Scoring
- No changes to existing workflow logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-leads/01-02-SUMMARY.md`
</output>

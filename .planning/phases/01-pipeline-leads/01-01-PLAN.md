---
phase: 01-pipeline-leads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/http.ts
  - C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/tests/smoke-phase1.sh
autonomous: true
requirements: [LEAD-02, LEAD-05]

must_haves:
  truths:
    - "n8n workflows can call leads functions via HTTP without 403"
    - "n8n workflows using Authorization Bearer header authenticate successfully"
    - "Duplicate leads by email/domain are rejected (dedup works via HTTP)"
  artifacts:
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/http.ts"
      provides: "HTTP allowlist with all leads + icpProfiles functions, dual auth header support"
      contains: "leads:batchUpsertLeads"
    - path: "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/tests/smoke-phase1.sh"
      provides: "Smoke tests for leads CRUD via HTTP endpoint"
      contains: "leads:batchUpsertLeads"
  key_links:
    - from: "SAAN/convex/http.ts"
      to: "api.leads.*"
      via: "ALLOWLIST dispatch"
      pattern: "leads:batchUpsertLeads.*api\\.leads\\.batchUpsertLeads"
    - from: "SAAN/convex/http.ts"
      to: "auth validation"
      via: "dual header check"
      pattern: "X-SAAN-Secret.*Authorization.*Bearer"
---

<objective>
Fix the 2 critical bugs in SAAN/convex/http.ts that block ALL n8n workflow communication with Convex leads functions. Without these fixes, no pipeline workflow can read or write leads.

Purpose: Unblock the entire Phase 1 pipeline by fixing (1) missing leads/icpProfiles functions in the HTTP allowlist (causes 403) and (2) auth header mismatch between scoring workflows (Bearer) and http.ts (X-SAAN-Secret) (causes 401).
Output: Working HTTP layer that accepts both auth headers and allows all leads + ICP functions.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-leads/01-RESEARCH.md

Source files to modify (in SAAN repo at C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/):
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/http.ts
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/leads.ts
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/icpProfiles.ts
@C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/tests/smoke-phase2.sh

<interfaces>
<!-- Existing exports from leads.ts that MUST be added to allowlist -->
Mutations: upsertLead, batchUpsertLeads, updateLeadStatus, updateLeadScore, enrichLead, updateOutreachStatus
Queries: getLeadByEmail, getLeadByCompany, getLeadsByStatus, getLeadsByScore, getLeadsBySource, getLeadsToEnrich, getLeadsToScore, getLeadsForOutreach, getLeadsStats

<!-- Existing exports from icpProfiles.ts that MUST be added to allowlist -->
Mutations: upsertIcpProfile
Queries: getActiveIcp, listIcpProfiles, getIcpForScoring
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix HTTP allowlist + auth header in http.ts</name>
  <files>C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/convex/http.ts</files>
  <action>
  Edit SAAN/convex/http.ts to fix both critical bugs:

  **Bug 1 — Allowlist:** Add ALL leads.ts and icpProfiles.ts exports to the ALLOWLIST object:
  ```
  // Leads CRUD
  "leads:upsertLead": api.leads.upsertLead,
  "leads:batchUpsertLeads": api.leads.batchUpsertLeads,
  "leads:getLeadByEmail": api.leads.getLeadByEmail,
  "leads:getLeadByCompany": api.leads.getLeadByCompany,
  "leads:getLeadsByStatus": api.leads.getLeadsByStatus,
  "leads:getLeadsByScore": api.leads.getLeadsByScore,
  "leads:getLeadsBySource": api.leads.getLeadsBySource,
  "leads:updateLeadStatus": api.leads.updateLeadStatus,
  "leads:updateLeadScore": api.leads.updateLeadScore,
  "leads:enrichLead": api.leads.enrichLead,
  "leads:getLeadsToEnrich": api.leads.getLeadsToEnrich,
  "leads:getLeadsToScore": api.leads.getLeadsToScore,
  "leads:updateOutreachStatus": api.leads.updateOutreachStatus,
  "leads:getLeadsForOutreach": api.leads.getLeadsForOutreach,
  "leads:getLeadsStats": api.leads.getLeadsStats,
  // ICP Profiles
  "icpProfiles:upsertIcpProfile": api.icpProfiles.upsertIcpProfile,
  "icpProfiles:getActiveIcp": api.icpProfiles.getActiveIcp,
  "icpProfiles:listIcpProfiles": api.icpProfiles.listIcpProfiles,
  "icpProfiles:getIcpForScoring": api.icpProfiles.getIcpForScoring,
  ```

  Also add leads queries + ICP queries to the QUERIES set:
  ```
  "leads:getLeadByEmail",
  "leads:getLeadByCompany",
  "leads:getLeadsByStatus",
  "leads:getLeadsByScore",
  "leads:getLeadsBySource",
  "leads:getLeadsToEnrich",
  "leads:getLeadsToScore",
  "leads:getLeadsForOutreach",
  "leads:getLeadsStats",
  "icpProfiles:getActiveIcp",
  "icpProfiles:listIcpProfiles",
  "icpProfiles:getIcpForScoring",
  ```

  **Bug 2 — Auth header:** Change the auth check to accept BOTH `X-SAAN-Secret` header AND `Authorization: Bearer <token>` header. Replace line 92:
  ```typescript
  const secret = request.headers.get("X-SAAN-Secret");
  ```
  With:
  ```typescript
  const secret = request.headers.get("X-SAAN-Secret")
    || request.headers.get("Authorization")?.replace("Bearer ", "");
  ```

  Also update the CORS headers to include Authorization:
  ```typescript
  "Access-Control-Allow-Headers": "Content-Type, X-SAAN-Secret, Authorization",
  ```
  </action>
  <verify>
    <automated>cd "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN" && npx convex typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>http.ts has 34 functions in allowlist (19 original + 15 leads + ICP), dual auth header support, CORS updated. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create smoke test for leads HTTP operations</name>
  <files>C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/tests/smoke-phase1.sh</files>
  <action>
  Create SAAN/tests/smoke-phase1.sh extending the pattern from smoke-phase2.sh. The test script should:

  1. Accept env vars: SAAN_CONVEX_SITE_URL and SAAN_API_SECRET
  2. Test with X-SAAN-Secret header:
     - Test leads:getLeadsStats (query, should return 200 with byCategory)
     - Test leads:batchUpsertLeads with 2 test leads (should return 200 with inserted count)
     - Test leads:getLeadByEmail to verify the inserted lead exists
     - Test leads:batchUpsertLeads with same email again (dedup: should return updated count)
     - Test leads:getLeadsToEnrich (should return leads with status "new")
  3. Test with Authorization Bearer header:
     - Test leads:getLeadsStats (should return 200, proving dual auth works)
  4. Test forbidden function (should return 403):
     - Test calling a function not in allowlist (e.g., "agents:deleteAgent")
  5. Print PASS/FAIL summary at end

  Use curl with -s flag. Each test should check HTTP status code and verify response contains expected fields.
  Mark the file executable (chmod +x).

  NOTE: These tests require a running Convex deployment. They are integration tests, not unit tests. The script should print clear instructions if env vars are missing.
  </action>
  <verify>
    <automated>test -f "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/tests/smoke-phase1.sh" && head -5 "C:/Users/Dell 5520/Documents/AgenticWorkflows/SAAN/tests/smoke-phase1.sh"</automated>
  </verify>
  <done>smoke-phase1.sh exists with 7 test cases covering: leads query, batch insert, dedup, enrich candidates, dual auth, and forbidden function. Script is executable and self-documenting.</done>
</task>

</tasks>

<verification>
- http.ts has leads:batchUpsertLeads in ALLOWLIST
- http.ts has dual auth header extraction (X-SAAN-Secret OR Authorization Bearer)
- http.ts CORS includes Authorization header
- QUERIES set includes all 12 query functions (9 leads + 3 ICP)
- Convex typecheck passes
- smoke-phase1.sh exists and is executable
</verification>

<success_criteria>
- Convex typecheck passes with no errors on modified http.ts
- All 15 leads functions + 4 ICP functions are in ALLOWLIST
- Auth header accepts both X-SAAN-Secret and Authorization Bearer
- Smoke test script covers batch insert, dedup, and dual auth
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-leads/01-01-SUMMARY.md`
</output>

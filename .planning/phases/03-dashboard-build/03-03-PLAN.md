---
phase: 03-dashboard-build
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/sdr.html
  - shared/content-builders.js
  - convex/leads.ts
autonomous: true
requirements: [DASH-02, DASH-04]

must_haves:
  truths:
    - "SDR ve su to-do list con leads asignados priorizados"
    - "SDR puede abrir panel de detalle de un lead con datos reales"
    - "Panel de detalle muestra score breakdown con 4 factores"
    - "Panel de detalle muestra audit trail cronologico"
    - "SDR puede cambiar estado del lead desde el panel"
    - "Datos de contacto son copiables/clickeables (tel, email, LinkedIn)"
  artifacts:
    - path: "app/sdr.html"
      provides: "SDR dashboard con to-do list y lead detail panel"
      contains: "queryConvex"
    - path: "shared/content-builders.js"
      provides: "SDR content builders added (buildSdrTareas, buildSdrLeadDetail)"
      contains: "buildSdrTareas"
  key_links:
    - from: "shared/content-builders.js"
      to: "convex/leads.ts"
      via: "queryConvex(api.leads.getLeadsByAssignee)"
      pattern: "getLeadsByAssignee|getLeadById"
    - from: "app/sdr.html"
      to: "shared/content-builders.js"
      via: "buildSdrTareas() on page load"
      pattern: "buildSdr"
---

<objective>
Conectar la vista SDR a datos reales de Convex. Implementar to-do list con leads asignados, panel de detalle de lead con score breakdown y audit trail, acciones de contacto (copiar tel/email/LinkedIn), y cambio de estado desde el panel.

Purpose: El SDR es el usuario que mas usa el dashboard en el dia a dia — necesita ver sus leads asignados y actuar rapidamente.
Output: SDR dashboard funcional con to-do list, lead detail panel, audit trail, y contact actions.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-build/03-RESEARCH.md
@.planning/phases/03-dashboard-build/03-01-SUMMARY.md

Reference mockup:
@mockups/sdr.html
@mockups/shared/interactions.js
</context>

<interfaces>
<!-- From Plan 01 — Convex queries for SDR -->
From convex/leads.ts:
```typescript
export const getLeadsByOrg = query({...});
export const getLeadById = query({...});          // args: {leadId} — returns full lead with auditTrail
export const updateLeadStatus = mutation({...});  // args: {leadId, nuevoEstado, subestado?}
```

From convex/users.ts:
```typescript
export const getUserRole = query({...});          // returns {rol: "sdr"|"vp"|"ceo"}
```

From shared/convex-client.js:
```javascript
window.queryConvex(queryFn, args)
window.mutateConvex(mutationFn, args)
```

From shared/content-builders.js (Plan 02 additions — may run in parallel):
```javascript
// If Plan 03 runs parallel to Plan 02, SDR builders are independent
// Both plans append to content-builders.js but to different builder namespaces
window.contentBuilders.sdr.*   // SDR namespace (this plan)
window.contentBuilders.ceo.*   // CEO namespace (Plan 02)
window.contentBuilders.vp.*    // VP namespace (Plan 02)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convex query for SDR leads + SDR content builders</name>
  <files>convex/leads.ts, shared/content-builders.js</files>
  <action>
1. Add to `convex/leads.ts` a query specifically for SDR view:
   - `getLeadsByAssignee` — takes no explicit userId arg. Uses `ctx.auth.getUserIdentity()` to get `subject` (Clerk userId). Queries leads where `asignadoA === currentUserId AND orgId === orgFromJWT`. Order by score descending (HOT first). Returns full lead objects including auditTrail.
   - Ensure `getLeadById` returns complete data including all enrichment fields (empresa, contacto, email, telefono, linkedin, industria, tamano, score, scoreCategory, scoreFactors, auditTrail).

2. Add SDR content builders to `shared/content-builders.js` under `window.contentBuilders.sdr`:

   - `buildSdrTareas()` — "Mis tareas de hoy":
     * Calls `queryConvex(api.leads.getLeadsByAssignee)`
     * Groups leads by priority: HOT asignados first, then en_progreso, then the rest
     * Renders to-do list matching mockup: colored left border (red=urgente/orange=alta/yellow=media/gray=baja), lead name, empresa, score badge, estado badge
     * Each item is clickable — opens lead detail panel
     * Per CONTEXT: SDR no rechaza leads — mark as Descartado via normal flow

   - `buildSdrMisLeads()` — "Mis leads asignados":
     * Same query but shows all leads in a simplified table (not grouped by priority)
     * Filterable by estado

   - `buildSdrHotPending()` — "Leads HOT pendientes":
     * Filter assignee leads by scoreCategory=HOT AND estado!=cerrado

   - `buildSdrStats()` — "Mis estadisticas":
     * Count by estado for assigned leads, show progress metrics

   - `buildSdrLeadDetail(leadId)` — Lead detail panel (slide-in from right):
     * Calls `queryConvex(api.leads.getLeadById, {leadId})`
     * Renders panel matching mockup exactly:
       - Header: lead name + empresa + score badge
       - Contact info: email (clickable mailto:), telefono (clickable tel:), LinkedIn (clickable href) + copy icon next to each
       - Score breakdown: 4 factors with proportional bars (same as mockup fillLeadPanel)
       - Estado: clickable badge to change status (same dropdown as VP)
       - Audit trail timeline: chronological list with dot + vertical line, each entry shows "accion — usuario — fecha relativa"
     * Copy functionality: clicking copy icon copies value to clipboard + shows "Copiado" toast
     * Per CONTEXT: tap for contact data opens native handler (tel->dialer, email->mail app, linkedin->browser)
     * Per CONTEXT: lead panel se abre por defecto con primer lead HOT
     * Panel opens with GSAP slide-in (x:400->0) matching mockup animation

3. Register all SDR builders on `window.contentBuilders.sdr`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f=fs.readFileSync('shared/content-builders.js','utf8'); console.log('SDR builders:', f.includes('buildSdrTareas') && f.includes('buildSdrLeadDetail')); const c=fs.readFileSync('convex/leads.ts','utf8'); console.log('getLeadsByAssignee:', c.includes('getLeadsByAssignee'));"</automated>
  </verify>
  <done>SDR to-do list shows assigned leads prioritized by score. Lead detail panel renders with real data including score breakdown and audit trail. Contact info is clickable and copiable. Status can be changed from the panel.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SDR page + lead detail panel interactions</name>
  <files>app/sdr.html, shared/interactions.js</files>
  <action>
1. Update `app/sdr.html`:
   - Ensure it loads auth.js, convex-client.js, content-builders.js, interactions.js in correct order
   - On page load after auth: call `buildSdrTareas()` as default view
   - Wire query buttons to SDR content builders (mis-tareas, mis-leads, hot-pending, reuniones, contactar, stats)
   - Add lead detail panel container (hidden by default, slide-in from right, overlay behind)

2. Implement lead detail panel interactions in `shared/interactions.js`:
   - `openLeadPanel(leadId)` — calls `buildSdrLeadDetail(leadId)`, shows panel with GSAP animation, adds overlay
   - `closeLeadPanel()` — GSAP slide-out, remove overlay
   - Click on overlay or X button closes panel
   - Status change inside panel: same dropdown pattern as VP, but calls mutation then refreshes panel data
   - After status change in panel: also refresh the to-do list behind the panel (update badge counts)

3. Per CONTEXT: Lead detail panel se abre por defecto con primer lead HOT para demostrar el patron. On page load, after loading to-do list, auto-open the first HOT lead's panel.

4. Per CONTEXT: SDR ve notificaciones solo dentro del dashboard (to-do list). When a new lead is assigned, it appears in the to-do list with a visual indicator ("Nuevo" badge that fades after first view). Store "seen" lead IDs in localStorage.

5. Ensure query buttons have dynamic badges with real counts (same pattern as CEO/VP).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f=fs.readFileSync('app/sdr.html','utf8'); console.log('Has auth guard:', f.includes('initAuth')); console.log('Has content-builders:', f.includes('content-builders')); console.log('Has panel container:', f.includes('lead-detail') || f.includes('panel'));"</automated>
  </verify>
  <done>SDR page loads with auth, shows to-do list by default, auto-opens first HOT lead panel. Lead detail panel slides in with full data, score breakdown, audit trail. Contact actions work (click to call/email/copy). Status changes update both panel and to-do list.</done>
</task>

</tasks>

<verification>
1. SDR page shows only leads assigned to the current user
2. To-do list is prioritized (HOT first)
3. Clicking a lead opens detail panel with real data
4. Score breakdown shows 4 factors with proportional bars
5. Audit trail shows chronological history
6. Contact info is clickable (tel, email, linkedin) and copiable
7. Status change from panel updates both panel and list
</verification>

<success_criteria>
- SDR to-do list displays assigned leads from Convex with correct prioritization
- Lead detail panel shows complete enriched data + score breakdown + audit trail
- Contact actions work: click opens native handler, copy button works
- Status changes from panel are immediate and reflected in the list
- First HOT lead auto-opens on page load
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-build/03-03-SUMMARY.md`
</output>

---
phase: 03-dashboard-build
plan: "05"
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - app/ceo.html
  - app/vp-ventas.html
  - app/sdr.html
  - shared/styles.css
  - shared/interactions.js
  - shared/content-builders.js
  - convex/icp.ts
autonomous: false
requirements: [DASH-06, DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-07]

must_haves:
  truths:
    - "Dashboard es usable en movil 375px (tabla VP es lista vertical, datos contacto copiables)"
    - "Empty state muestra 'Tu pipeline esta vacio' + command bar activo"
    - "ICP wizard permite configurar 3-4 parametros antes de activar pipeline"
    - "VP puede activar pipeline desde el dashboard"
    - "Todas las 3 vistas funcionan end-to-end con datos reales"
  artifacts:
    - path: "shared/styles.css"
      provides: "Mobile responsive CSS (375px, 768px breakpoints)"
      contains: "@media"
    - path: "convex/icp.ts"
      provides: "ICP configuration storage"
      exports: ["saveIcpConfig"]
  key_links:
    - from: "shared/interactions.js"
      to: "n8n webhook"
      via: "fetch(N8N_WEBHOOK_URL) to activate pipeline"
      pattern: "N8N_WEBHOOK|activarPipeline"
    - from: "app/vp-ventas.html"
      to: "convex/icp.ts"
      via: "mutateConvex(api.icp.saveIcpConfig)"
      pattern: "saveIcpConfig"
---

<objective>
Completar el dashboard con mobile responsive, empty states, ICP wizard, pipeline activation, y verificacion final de todas las funcionalidades end-to-end.

Purpose: El dashboard debe ser usable en movil para SDRs en terreno, manejar el caso de 0 leads elegantemente, y permitir al VP activar el pipeline de forma self-service.
Output: Dashboard completo, responsive, con empty states, ICP wizard, y pipeline activation. Listo para Phase 4 (compliance) y Phase 5 (onboarding).
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-build/03-RESEARCH.md
@.planning/phases/03-dashboard-build/03-01-SUMMARY.md
@.planning/phases/03-dashboard-build/03-02-SUMMARY.md
@.planning/phases/03-dashboard-build/03-03-SUMMARY.md
@.planning/phases/03-dashboard-build/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile responsive + empty states + ICP wizard + pipeline activation</name>
  <files>shared/styles.css, shared/interactions.js, shared/content-builders.js, app/ceo.html, app/vp-ventas.html, app/sdr.html, convex/icp.ts</files>
  <action>
1. Mobile responsive CSS in `shared/styles.css`:
   - Breakpoint `@media (max-width: 768px)`:
     * Sidebar collapses to hamburger menu (60px strip or hidden)
     * Topbar becomes compact (title only, actions in menu)
     * Content takes 100% width
     * KPI cards: 2-column grid (not 4)
     * VP pipeline table: convert to vertical card list per CONTEXT — "lista simplificada vertical (nombre + score badge + estado). Tap para ver detalle"
     * SDR to-do list: full width cards
     * Command bar: Claude's discretion per CONTEXT — implement as FAB (floating action button) in bottom-right corner that opens a bottom sheet. This avoids the Pitfall 5 from RESEARCH (command bar consuming too much vertical space on mobile).
     * Lead detail panel: full-screen overlay on mobile (not side panel)
   - Breakpoint `@media (max-width: 480px)`:
     * Single column everything
     * Larger touch targets (min 44px)
   - Per CONTEXT: "Datos de contacto SDR: tap para abrir (tel -> dialer, email -> correo, LinkedIn -> app/web) + icono copiar al lado". Ensure contact links use proper `href` (tel:, mailto:, https://linkedin.com/...) and copy buttons have 44px touch targets.
   - Per CONTEXT: "Cambio de estado desde movil: si, acciones basicas de campo disponibles"

2. Empty states in `shared/content-builders.js`:
   - When any query returns 0 results:
     * Show clean screen: Sisteco icon + "Tu pipeline esta vacio" (or context-appropriate message)
     * Command bar stays active with normal query buttons
     * Per CONTEXT: "Queries vacias: mismos botones de siempre, al clickear muestran 'Aun no hay datos. Proximo batch: [fecha]' con fecha estimada"
     * Estimate next batch date: if pipeline is active, show next PhantomBuster run date (Mon/Wed/Fri 07:00). If not active, show "Activa tu pipeline para empezar".
   - Per CONTEXT: "Texto limpio sin datos ficticios ni skeletons fantasma" — empty state is clean copy, not skeleton placeholders.

3. VP dual mode in `shared/content-builders.js`:
   - Per CONTEXT: "VP dual: sin SDRs, el VP trabaja como SDR (ve leads directamente). Cuando invite equipo, cambia a vista de gestion."
   - Check team size: if `getTeamMembers` returns only the VP (1 member), show leads directly in the VP view (like SDR view). Hide "Asignar" functionality.
   - If team has 2+ members: show normal VP management view with assignment.

4. ICP wizard + pipeline activation:
   - Create `convex/icp.ts`:
     * `saveIcpConfig` mutation — stores ICP config `{orgId, industria: string, tamano: string, ubicacion: string, keywords: string}` in a new `icpConfigs` table. Uses orgId from JWT.
     * `getIcpConfig` query — returns current ICP for org.
   - In `shared/content-builders.js`, add `buildIcpWizard()`:
     * Step 1: "Que industria buscas?" — dropdown with Chilean industries (Tecnologia, Fintech, Retail, Manufactura, Servicios, Construccion, Mineria, Otro)
     * Step 2: "Que tamano de empresa?" — radio buttons (10-50, 50-200, 200-1000, 1000+ empleados)
     * Step 3: "Ubicacion preferida?" — dropdown (Santiago, Regiones, Todo Chile)
     * Step 4: "Keywords opcionales?" — text input for search terms
     * Submit: saves ICP to Convex, then calls pipeline activation
   - Pipeline activation:
     * `activarPipeline(icpData)` — POST to n8n webhook URL with `{orgId, icp: icpData}`
     * n8n webhook URL stored in env var or config
     * After activation: show success message "Pipeline activado. Primeros leads en 24-48 horas."
   - Per CONTEXT: "Activacion del pipeline: VP activa desde el dashboard (boton/query en command bar)"
   - Register "Activar pipeline" as a VP command bar query
   - Per CONTEXT: "Wizard ICP breve antes de activar: 3-4 preguntas"

5. Comparison temporal toggle:
   - Per CONTEXT: "Comparacion temporal: toggle vs semana anterior y mes anterior. Solo esos 2 periodos en MVP"
   - Add toggle UI to CEO KPIs view: small pill buttons "vs semana" / "vs mes"
   - When toggled: fetch stats for previous period and show delta (arrow up/down + percentage) on each KPI card
  </action>
  <verify>
    <automated>node -e "const css=require('fs').readFileSync('shared/styles.css','utf8'); console.log('Mobile breakpoint:', css.includes('max-width: 768px')); const js=require('fs').readFileSync('shared/content-builders.js','utf8'); console.log('Empty states:', js.includes('pipeline esta vacio') || js.includes('no hay datos')); console.log('ICP wizard:', js.includes('buildIcpWizard'));"</automated>
  </verify>
  <done>Dashboard is responsive at 768px and 375px. VP pipeline table becomes card list on mobile. Empty states show clean messages. ICP wizard collects 4 parameters. Pipeline activation calls n8n webhook. Temporal comparison toggles between week/month.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificacion final del dashboard completo</name>
  <files>app/ceo.html, app/vp-ventas.html, app/sdr.html</files>
  <action>
  Presentar el dashboard completo al usuario para verificacion visual y funcional. Todas las funcionalidades deben estar operativas: auth, 3 vistas de rol, command bar con Gemini fallback, PDF, mobile responsive, empty states, ICP wizard, pipeline activation.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification"</automated>
  </verify>
  <done>Usuario aprueba el dashboard completo con las 3 vistas funcionando end-to-end.</done>
  <what-built>Dashboard funcional completo: 3 vistas (CEO, VP, SDR) con datos reales de Convex, auth via Clerk, command bar con Gemini fallback, PDF generation, mobile responsive, empty states, ICP wizard, pipeline activation.</what-built>
  <how-to-verify>
    1. Abrir http://localhost:3000/app/login.html — verificar que login con Clerk funciona
    2. Despues del login, verificar redireccion a la vista correcta segun rol
    3. Vista CEO: verificar KPIs con datos reales, funnel, comparacion temporal
    4. Vista VP: verificar pipeline table con filtros, asignar lead, cambiar estado
    5. Vista SDR: verificar to-do list, abrir panel de lead, copiar datos de contacto
    6. Command bar: escribir query no reconocida — verificar respuesta de Gemini
    7. Command bar: escribir "generar reporte PDF" — verificar descarga con branding Sisteco
    8. Reducir ventana a 375px — verificar layout mobile (tabla VP como cards, command bar como FAB)
    9. Verificar empty state: si no hay leads, muestra "Tu pipeline esta vacio"
    10. VP: ejecutar "Activar pipeline" — verificar ICP wizard (4 pasos)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Mobile: 375px viewport shows card list for VP, FAB for command bar, full-screen lead panel
2. Empty state: 0 leads shows clean message + active command bar
3. ICP wizard: 4 steps (industria, tamano, ubicacion, keywords) saves to Convex
4. Pipeline activation: n8n webhook receives POST with ICP data
5. VP dual: with no team, VP sees leads directly (SDR-like view)
6. Temporal comparison: CEO can toggle vs semana/mes with delta arrows
7. All 3 role pages: fully functional with real data end-to-end
</verification>

<success_criteria>
- Dashboard responsive on mobile (375px and 768px breakpoints)
- Empty states are clean and informative
- ICP wizard collects configuration and saves to Convex
- Pipeline activation triggers n8n webhook
- VP dual mode works correctly (solo VP vs VP+team)
- Temporal comparison works for CEO KPIs
- Human verification: all 3 vistas approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-build/03-05-SUMMARY.md`
</output>

---
phase: 04-leads-agent
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - SAAN/skills/lead-scraper/skill.md
  - SAAN/skills/lead-scraper/script.js
  - SAAN/dashboard/index.html
autonomous: false
requirements: [LEAD-01, LEAD-02, LEAD-03, LEAD-04, LEAD-05, LEAD-06, LEAD-07, LEAD-08]

must_haves:
  truths:
    - "El skill lead-scraper esta actualizado para usar Firecrawl en vez de Apollo/Hunter"
    - "El dashboard muestra seccion de leads con conteos por categoria"
    - "El flujo completo funciona: discover -> enrich -> score -> task CEO"
  artifacts:
    - path: "SAAN/skills/lead-scraper/skill.md"
      provides: "SOP actualizado del skill sin Apollo ni Hunter"
    - path: "SAAN/skills/lead-scraper/script.js"
      provides: "Script actualizado usando Firecrawl para discovery"
    - path: "SAAN/dashboard/index.html"
      provides: "Seccion de leads en dashboard CEO"
      contains: "leads"
  key_links:
    - from: "SAAN/dashboard/index.html"
      to: "*.convex.site/api/call"
      via: "Convex client consultando leads:getLeadsStats"
      pattern: "getLeadsStats"
---

<objective>
Actualizar el skill lead-scraper para alinearlo con el stack real (Firecrawl, no Apollo/Hunter), agregar seccion de leads al dashboard, y documentar el flujo completo.

Purpose: Cerrar la fase con assets actualizados y visibilidad del CEO sobre los leads descubiertos.
Output: Skill actualizado + dashboard con leads + verificacion del flujo end-to-end.
</objective>

<execution_context>
@C:/Users/Dell 5520/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell 5520/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@SAAN/skills/lead-scraper/skill.md
@SAAN/skills/lead-scraper/script.js
@SAAN/dashboard/index.html
@.planning/phases/04-leads-agent/04-01-SUMMARY.md
@.planning/phases/04-leads-agent/04-02-SUMMARY.md
@.planning/phases/04-leads-agent/04-03-SUMMARY.md

<interfaces>
<!-- Queries disponibles para el dashboard -->
leads:getLeadsStats() => { hot, warm, nurture, skip, total }
leads:getLeadsByStatus({ status, limit? }) => Lead[]
leads:getLeadsByScore({ minScore, limit? }) => Lead[]
agents:getAgentState({ agentId: "leads" }) => AgentState
agents:getRecentMemory({ agentId: "leads", memoryType: "report", limit: 5 }) => Memory[]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar skill lead-scraper para usar Firecrawl</name>
  <files>SAAN/skills/lead-scraper/skill.md, SAAN/skills/lead-scraper/script.js</files>
  <action>
**skill.md:** Reescribir el SOP completo:
- Eliminar TODAS las referencias a Apollo.io y Hunter.io
- El flujo ahora es: Firecrawl Search (discovery) -> Firecrawl Scrape (enrichment) -> Gemini Flash Lite (scoring)
- Actualizar la seccion "requires" a: ["firecrawl", "gemini-api"]
- Actualizar "APIs Necesarias" con Firecrawl Search y Scrape endpoints
- Mantener la seccion de compliance (Ley 19.628 y Ley 21.719) intacta
- Mantener los filtros optimos para mercado chileno
- Actualizar "Waterfall de Enriquecimiento" a: 1. Firecrawl Search, 2. Firecrawl Scrape, 3. Gemini Scoring, 4. ScrapingBee (fallback)
- Documentar los 3 workflows n8n creados: saan-leads-discover, saan-leads-enrich, saan-leads-score

**script.js:** Reescribir el script principal:
- Eliminar funciones de PhantomBuster (launchPhantom, waitForPhantom, getPhantomOutput)
- Eliminar funcion enrichWithScrapingBee (mover a fallback comentado)
- Crear funcion `discoverWithFirecrawl(apiKey, query, limit)` que llama a POST /v1/search
- Crear funcion `enrichWithFirecrawl(apiKey, url)` que llama a POST /v1/scrape
- Mantener funciones `parseQuery` y `scoreLead` actualizadas
- El pipeline principal: parseQuery -> discoverWithFirecrawl -> enrichWithFirecrawl -> scoreLead
- Exportar: { discoverWithFirecrawl, enrichWithFirecrawl, parseQuery, scoreLead }
- Mantener rate limiting (2s entre discovery, 3s entre scrapes)
  </action>
  <verify>
    <automated>node -e "const s=require('./SAAN/skills/lead-scraper/script.js'); console.log('exports:', Object.keys(s).join(', ')); console.log('has discoverWithFirecrawl:', typeof s.discoverWithFirecrawl === 'function'); console.log('has enrichWithFirecrawl:', typeof s.enrichWithFirecrawl === 'function')"</automated>
  </verify>
  <done>skill.md sin menciones de Apollo/Hunter, con Firecrawl como fuente principal. script.js exporta discoverWithFirecrawl y enrichWithFirecrawl. No hay imports de PhantomBuster.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar seccion de leads al dashboard CEO</name>
  <files>SAAN/dashboard/index.html</files>
  <action>
Agregar una nueva seccion al dashboard existente (`SAAN/dashboard/index.html`) que muestre el estado del Leads Agent. Insertar DESPUES de las secciones existentes, ANTES del cierre del main/body.

La seccion debe incluir:

1. **Header:** "Target Leads Agent" con indicador de estado (active/idle/error, consultado de agentsState)

2. **Tarjetas de metricas** (grid de 4 columnas en desktop, 2 en mobile):
   - HOT leads (fondo verde #c5ed36, numero grande)
   - WARM leads (fondo amarillo)
   - NURTURE leads (fondo azul claro)
   - SKIP leads (fondo gris)
   - Total leads (numero aparte)

3. **Lista de HOT leads recientes** (max 5):
   - Nombre empresa
   - Score
   - Razonamiento del score (truncado a 100 chars)
   - Link al sitio web
   - Boton "Contactar" (placeholder, no funcional en esta fase)

4. **Ultimo run del agente:**
   - Fecha/hora del ultimo discovery run
   - Fecha/hora del ultimo scoring run
   - Leads descubiertos hoy
   - Leads scored hoy

Usar el Convex client ya existente en el dashboard (CDN). Agregar queries via el ConvexClient que ya esta instanciado.

Estilo: seguir el dark mode existente del dashboard. Usar la paleta de Sisteco (#F8F7F5 para texto sobre dark, #c5ed36 para acentos, #111111 para fondos).

Si el dashboard usa Convex client directo (no HTTP), las queries serian:
- `api.leads.getLeadsStats` para conteos
- `api.leads.getLeadsByScore` con minScore 80 para HOT leads recientes
- `api.agents.getAgentState` con agentId "leads"

Si usa HTTP (via fetch al Convex site), adaptar las llamadas.

Revisar el dashboard existente para entender el patron y seguirlo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('./SAAN/dashboard/index.html','utf-8'); console.log('has leads section:', html.includes('leads') || html.includes('Leads')); console.log('has getLeadsStats:', html.includes('getLeadsStats') || html.includes('LeadsStats')); console.log('has HOT:', html.includes('HOT'))"</automated>
  </verify>
  <done>Dashboard muestra seccion de Leads Agent con 4 tarjetas de metricas, lista de HOT leads, y estado del agente. Sigue el estilo dark mode existente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Fase 4 completa — Leads Agent con:
1. Tabla leads en Convex con deduplicacion (Plan 01)
2. Workflow discover: busca prospectos via Firecrawl diariamente (Plan 02)
3. Workflow enrich: enriquece leads con datos del sitio web cada 2h (Plan 02)
4. Workflow score: clasifica leads con Gemini cada 3h, crea tareas HOT (Plan 03)
5. Skill actualizado sin Apollo/Hunter (Plan 04)
6. Dashboard con seccion de leads (Plan 04)
  </what-built>
  <how-to-verify>
1. Verificar que `npx convex dev --once` en SAAN/ compila sin errores
2. Abrir SAAN/dashboard/index.html — verificar que la seccion de Leads aparece con el estilo correcto
3. Revisar los 3 archivos JSON de workflows en SAAN/n8n-workflows/:
   - saan-leads-discover.json — debe tener trigger cron, Firecrawl Search, batch upsert
   - saan-leads-enrich.json — debe tener trigger cron, get leads to enrich, Firecrawl Scrape
   - saan-leads-score.json — debe tener trigger cron, Gemini API, create task for HOT
4. Verificar que skill.md NO menciona Apollo ni Hunter
5. Verificar que script.js exporta discoverWithFirecrawl y enrichWithFirecrawl
6. (Opcional) Importar un workflow en n8n para verificar que se importa correctamente

**Para probar el flujo completo (requiere API keys configuradas):**
- Configurar en n8n: CONVEX_SITE_URL, SAAN_API_SECRET, FIRECRAWL_API_KEY, GEMINI_API_KEY
- Ejecutar manualmente saan-leads-discover
- Esperar a que inserte leads
- Ejecutar manualmente saan-leads-enrich
- Ejecutar manualmente saan-leads-score
- Verificar en dashboard que aparecen leads con scores
  </how-to-verify>
  <resume-signal>Escribe "approved" si todo se ve correcto, o describe los problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
1. skill.md y script.js actualizados sin Apollo/Hunter
2. Dashboard muestra seccion de leads funcional
3. Flujo completo: discover -> enrich -> score -> task CEO documentado y funcional
</verification>

<success_criteria>
- Skill lead-scraper alineado con stack real (Firecrawl + Gemini)
- Dashboard CEO muestra metricas de leads y HOT leads recientes
- El usuario confirma visualmente que todo se ve correcto
- Todos los archivos de la fase existen y compilan
</success_criteria>

<output>
Crear `.planning/phases/04-leads-agent/04-04-SUMMARY.md`
</output>
